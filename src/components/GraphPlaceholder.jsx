import React from "react";

export default function GraphPlaceholder() {
  return (
    <div
      style={{
        border: "1px dashed #9ca3af",
        padding: 16,
        borderRadius: 12,
        marginTop: 16,
        textAlign: "center",
        color: "#6b7280",
        fontSize: ".9rem",
      }}
    >
      Graphique à venir (intégration Recharts ou Chart.js possible ici)
    </div>
  );
}
